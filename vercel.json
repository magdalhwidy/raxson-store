cat > vercel.json << 'EOF'
{
  "version": 2,
  "rewrites": [
    { "source": "/api/:path*", "destination": "https://raxson.freepage.cc/api/:path*" }
  ],
  "routes": [
    { "src": "/admin/(.*)", "dest": "/admin/$1" },
    { "src": "/(.*)", "dest": "/Front/$1" }
  ]
}
EOF
